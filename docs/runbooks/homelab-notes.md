## Notes
- Lingarr migrated from SQLite to MariaDB (lingarr-db on :3307).
- OpenClaw node relay watchdog enabled via LaunchAgent.
- Tailscale SSH enabled (RunSSH=true).
- Media + monitoring stack now has declarative compose in `services/media/docker-compose.yml`.
- Oracle gateway runtime canonical path is `/opt/openclaw`.
