# Mac mini

Conteudo especifico do host local do homelab.

- `dotfiles/`: shell profiles versionados.
- `launchagents/`: jobs do launchd (automacao local no macOS).
- `configs/`: snapshots de configuracao de energia e Tailscale.
